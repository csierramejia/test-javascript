
// It only works in the object window of the browser
//const valor = prompt( 'Enter a value');

// It only works in the object window of the browser
//const opt = confirm( 'Are you sure?' );
//console.log( opt );